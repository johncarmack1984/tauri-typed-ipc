# Track the current stable toolchain. examples/src-tauri inherits this too:
# rustup resolves by directory walk, not by cargo workspace.
[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
